!!! Strict
%html{:xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %meta{:content => "", :name => "keywords"}
    %meta{:content => "", :name => "description"}
    %meta{:content => "text/html; charset=utf-8", "http-equiv" => "content-type"}
    %title RubyusergroupBe
    = stylesheet_link_tag 'reset'
    = stylesheet_link_tag 'application'
    = stylesheet_link_tag 'flash_messages'
    = stylesheet_link_tag 'forms'
    = stylesheet_link_tag 'coderay'
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    #top-header
      #actions-navigation
        %ul
          - if user_signed_in?
            %li= link_to current_user.email, edit_user_registration_path
            %li= link_to "Abmelden", destroy_user_session_path
          - else
            %li= link_to "Registrieren", new_user_registration_path
            %li= link_to "Anmelden", new_user_session_path
    #header
      #inner-header
        #title
          %h1 Bern
          %h2 Ruby User Group
    #navigation
      = render "layouts/navigation"
    #container
      #content
        - if @title
          #content-header
            %h2= @title
        #flash_messages
          - flash.each do |type, message|
            %div{:class => "flash #{type}"}= message
        = yield
        #footer
          %p= raw t('layout.footer.text')
      - if user_signed_in? || home_page_active?
        #sidebar
          = render_sidebar


